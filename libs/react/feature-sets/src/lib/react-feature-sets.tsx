import React from 'react';

import './react-feature-sets.css';

/* eslint-disable-next-line */
export interface ReactFeatureSetsProps {}

export const ReactFeatureSets = (props: ReactFeatureSetsProps) => {
  return (
    <div>
      <h1>Welcome to react-feature-sets component!</h1>
    </div>
  );
};

export default ReactFeatureSets;
